import React from 'react';
import styles from '../style';

const Features = () => {
  return (
    <section id='features' className={`flex md:flex-row flex-col ${styles.paddingY} featuresComponent`}>
      a
    </section>
  );
};

export default Features;
